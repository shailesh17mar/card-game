# card-game

# Installation
RUN `yarn install`

# Build and Run
RUN `yarn build`
RUN `yarn start`

OR
RUN `yarn debug`

# Run tests with coverage
RUN `yarn test`


# Run test watch mode
RUN `yarn test:watch`
